import './App.css';

function Form({ inputValue }) {
  return (
    <div className="App">
      <header id="App-header">
        <p>
            Guess the map!
        </p>
      </header>
    </div>
  );
}

export default Form;
